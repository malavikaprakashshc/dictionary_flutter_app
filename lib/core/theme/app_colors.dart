import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primaryBlue = Color(0xFFb5cad4);
  static const Color primaryPink = Color(0xFFf5bebb);
  static const Color primaryLightBrown = Color.fromARGB(255, 144, 108, 82);
  static const Color primaryBrown = Color.fromARGB(255, 93, 62, 47);

  // Accent Colors (gold tone from logo)
  static const Color accentGold = Color(0xFFD4AF37);
  static const Color accentLightGold = Color(0xFFE6C76A);

  // Background Colors
  static const Color backgroundLight = Color(0xFFF5F6F8);
  static const Color backgroundWhite = Color(0xFFFFFFFF);
  static const Color backgroundDark = Color(0xFF121212);

  // Text Colors
  static const Color textPrimary = Color(0xFF1C1C1E);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textLight = Color(0xFFFFFFFF);

  // Card / Surface
  static const Color cardColor = Color(0xFFFFFFFF);
  static const Color cardShadow = Color(0x1A000000);

  // Border / Divider
  static const Color border = Color(0xFFE5E7EB);
  static const Color divider = Color(0xFFEEEEEE);

  // Status Colors
  static const Color success = Color(0xFF22C55E);
  static const Color error = Color(0xFFEF4444);
  static const Color warning = Color(0xFFF59E0B);

  // Chip Colors (synonyms / antonyms)
  static const Color chipBackground = Color(0xFFE9EEF5);
  static const Color chipText = Color(0xFF374151);
}
